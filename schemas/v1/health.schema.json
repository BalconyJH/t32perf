{
  "$defs": {
    "HealthIssue": {
      "description": "A diagnosed health issue produced by host policy.",
      "properties": {
        "artifact_id": {
          "description": "Related manifest artifact identifier.",
          "type": [
            "string",
            "null"
          ]
        },
        "code": {
          "description": "Stable machine-readable issue code.",
          "type": "string"
        },
        "end_ns": {
          "description": "End of the affected session-relative interval.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "evidence": {
          "additionalProperties": true,
          "description": "Structured evidence retained for diagnosis and audit.",
          "type": "object"
        },
        "message": {
          "description": "Concise human-readable diagnosis.",
          "type": "string"
        },
        "record": {
          "description": "Related source record number.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "severity": {
          "$ref": "#/$defs/HealthSeverity",
          "description": "Policy-assigned issue severity."
        },
        "source": {
          "description": "Component or source observation responsible for the issue.",
          "type": "string"
        },
        "start_ns": {
          "description": "Start of the affected session-relative interval.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "code",
        "severity",
        "source",
        "evidence",
        "message"
      ],
      "type": "object"
    },
    "HealthObservation": {
      "description": "A source fact consumed by the versioned host health policy.",
      "properties": {
        "artifact_id": {
          "description": "Related manifest artifact identifier.",
          "type": [
            "string",
            "null"
          ]
        },
        "code": {
          "description": "Stable machine-readable observation code.",
          "type": "string"
        },
        "end_ns": {
          "description": "End of the affected session-relative interval.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        },
        "evidence": {
          "additionalProperties": true,
          "description": "Structured source evidence used by host policy.",
          "type": "object"
        },
        "record": {
          "description": "Related source record number.",
          "format": "uint64",
          "minimum": 0,
          "type": [
            "integer",
            "null"
          ]
        },
        "source": {
          "description": "Adapter, parser, or analysis component that observed the fact.",
          "type": "string"
        },
        "start_ns": {
          "description": "Start of the affected session-relative interval.",
          "format": "int64",
          "type": [
            "integer",
            "null"
          ]
        }
      },
      "required": [
        "code",
        "source",
        "evidence"
      ],
      "type": "object"
    },
    "HealthSeverity": {
      "description": "Severity assigned to a host-policy issue.",
      "oneOf": [
        {
          "const": "info",
          "description": "Informational evidence that does not reduce trust.",
          "type": "string"
        },
        {
          "const": "warning",
          "description": "A warning that may constrain interpretation.",
          "type": "string"
        },
        {
          "const": "error",
          "description": "A serious problem that invalidates affected quantitative conclusions.",
          "type": "string"
        },
        {
          "const": "fatal",
          "description": "A capture-wide failure that makes the trace unusable.",
          "type": "string"
        }
      ]
    },
    "HealthVerdict": {
      "description": "Host-policy verdict for the trustworthiness of a capture.",
      "oneOf": [
        {
          "const": "VALID",
          "description": "Quantitative results and strict comparisons are permitted.",
          "type": "string"
        },
        {
          "const": "DEGRADED",
          "description": "Timeline inspection is permitted, but strict comparison is not.",
          "type": "string"
        },
        {
          "const": "INVALID",
          "description": "Only diagnostic output is trustworthy.",
          "type": "string"
        }
      ]
    },
    "MetricSupport": {
      "description": "Support matrix covering all required timeline and timing metrics.",
      "properties": {
        "active": {
          "$ref": "#/$defs/MetricSupportEntry",
          "description": "Support for active function duration excluding preemption."
        },
        "call_count": {
          "$ref": "#/$defs/MetricSupportEntry",
          "description": "Support for exact or estimated function call counts."
        },
        "elapsed": {
          "$ref": "#/$defs/MetricSupportEntry",
          "description": "Support for wall-clock elapsed function duration."
        },
        "function_timeline": {
          "$ref": "#/$defs/MetricSupportEntry",
          "description": "Support for reconstructed function timeline slices."
        },
        "isr_timeline": {
          "$ref": "#/$defs/MetricSupportEntry",
          "description": "Support for the interrupt execution timeline."
        },
        "resource_counters": {
          "$ref": "#/$defs/MetricSupportEntry",
          "default": {
            "reasons": [
              "resource_counter_support_not_recorded"
            ],
            "support": "unavailable"
          },
          "description": "Support for explicitly defined numeric resource counters."
        },
        "self": {
          "$ref": "#/$defs/MetricSupportEntry",
          "description": "Support for self active function duration excluding children."
        },
        "task_timeline": {
          "$ref": "#/$defs/MetricSupportEntry",
          "description": "Support for the task execution timeline."
        }
      },
      "required": [
        "function_timeline",
        "call_count",
        "elapsed",
        "active",
        "self",
        "task_timeline",
        "isr_timeline"
      ],
      "type": "object"
    },
    "MetricSupportEntry": {
      "description": "Support and explanatory reasons for one metric.",
      "properties": {
        "reasons": {
          "description": "Stable or human-readable reasons for the declared level.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "support": {
          "$ref": "#/$defs/MetricSupportLevel",
          "description": "Available support level."
        }
      },
      "required": [
        "support",
        "reasons"
      ],
      "type": "object"
    },
    "MetricSupportLevel": {
      "description": "The support level for one analysis metric.",
      "oneOf": [
        {
          "const": "exact",
          "description": "The capture directly supports exact computation.",
          "type": "string"
        },
        {
          "const": "inferred",
          "description": "The metric can be deterministically inferred with documented caveats.",
          "type": "string"
        },
        {
          "const": "statistical",
          "description": "Only a statistical estimate is available.",
          "type": "string"
        },
        {
          "const": "unavailable",
          "description": "The capture cannot support this metric.",
          "type": "string"
        }
      ]
    }
  },
  "$id": "t32perf.health/v1",
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Versioned host-policy health report for one session.",
  "properties": {
    "issues": {
      "description": "Diagnosed policy issues.",
      "items": {
        "$ref": "#/$defs/HealthIssue"
      },
      "type": "array"
    },
    "metric_support": {
      "$ref": "#/$defs/MetricSupport",
      "description": "Metric-specific support determined by the same policy evaluation."
    },
    "observations": {
      "description": "Raw facts supplied to host policy.",
      "items": {
        "$ref": "#/$defs/HealthObservation"
      },
      "type": "array"
    },
    "policy_version": {
      "description": "Version of the host policy that derived issues and verdict.",
      "type": "string"
    },
    "schema": {
      "const": "t32perf.health/v1",
      "description": "The health schema version.",
      "type": "string"
    },
    "session_id": {
      "description": "Session evaluated by the policy.",
      "type": "string"
    },
    "verdict": {
      "$ref": "#/$defs/HealthVerdict",
      "description": "Capture-wide policy verdict."
    }
  },
  "required": [
    "schema",
    "session_id",
    "verdict",
    "policy_version",
    "observations",
    "issues",
    "metric_support"
  ],
  "title": "HealthReport",
  "type": "object"
}
